import data from "./deck.json";

export async function getDeck() {
  // Do your data fetching here, then return the data.
  // In this example, we're just returning the JSON data directly.
  return data;
}
